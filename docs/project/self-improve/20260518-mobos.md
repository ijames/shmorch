### Self-Improve Proposals — 2026-05-18 | Project: mobos

#### Proposal 1: Broad text sweeps need a post-sweep test gate
**Pattern:** The emdash (—→--) sweep across 73 files accidentally changed two code values: `MaxExecQuantity::__toString()` string `'MAX(exec)'`→`'MAX(I)'` and the loop condition `$count < $loop_max`→`$count++ <= $loop_max`. Both were caught only by running tests after the fact, requiring a fix commit.
**Frequency:** Once this session, but sweeps will recur.
**File:** `~/.claude/skills/shmorch/workflows/build.md` (Definition of Done section)
**Change:** Add to DoD checklist: "If this commit includes a broad text replacement (> 5 files), run the full test suite before committing — sweeps can silently change string literals and operator expressions."
**Improvement:** Catches accidental code-value mutations from sweeps before commit, not after.

### No-action observations
- SESSION_START without matching SESSION_END appears frequently in timelog — artifact of context window splits, not a workflow gap.
- writeTradeCSVRow refactor-then-partial-revert: single occurrence, triggered by mid-session behavior clarification. Not a pattern yet.
