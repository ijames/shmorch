### Self-Improve Proposals — 2026-08-09b | Project: shmorch

#### Proposal 1: Sync `self-improve.md`'s EXPECTED_DOCS scaffold list with `auto-update.md`
**Pattern:** The two files maintain parallel `EXPECTED_DOCS` lists used to detect canonical-vs-drift doc directories. PR #99 (merged 2026-08-09, same-day) added `docs/project/plan`, `docs/project/prioritizer`, `docs/project/documentarian`, and `docs/technology/infrastructure` to `workflows/auto-update.md`'s list (line 160) because they'd become canonical and were being wrongly flagged as drift. `workflows/self-improve.md`'s own scaffold-check (line 69) still has the old, narrower list and is missing all four:
- `workflows/self-improve.md:69`: `EXPECTED_DOCS="docs docs/project docs/project/tracks docs/project/schedule docs/project/process docs/product docs/product/strategy docs/product/design docs/product/features docs/product/decisions docs/technology docs/technology/architecture docs/technology/development docs/technology/decisions docs/reference docs/reference/instructions docs/reference/research docs/inbox"`
- `workflows/auto-update.md:160` (current, correct): adds `docs/project/plan`, `docs/project/prioritizer`, `docs/project/documentarian`, `docs/technology/infrastructure`.

**Frequency:** Confirmed via direct diff of the two lines (not a multi-occurrence timelog pattern, but pre-flagged evidence verified true — the two lists have drifted out of sync at least once before and nothing keeps them in sync).
**File:** `workflows/self-improve.md`, line 69.
**Change:** Replace the `EXPECTED_DOCS` string with the one in `auto-update.md:160` (add `docs/project/plan`, `docs/project/prioritizer`, `docs/project/documentarian`, `docs/technology/infrastructure`), so both files agree:
```
EXPECTED_DOCS="docs docs/project docs/project/plan docs/project/tracks docs/project/schedule docs/project/process docs/project/prioritizer docs/project/documentarian docs/product docs/product/strategy docs/product/design docs/product/features docs/product/decisions docs/technology docs/technology/architecture docs/technology/development docs/technology/infrastructure docs/technology/decisions docs/reference docs/reference/instructions docs/reference/research docs/inbox"
```
Consider a follow-up note (not required for this fix) that the two lists are duplicated and should either be sourced from one shared location or cross-referenced with a comment ("keep in sync with workflows/auto-update.md:160") to prevent the next scaffold addition from drifting again.
**Improvement:** `self-improve`'s own scaffold-drift check will stop flagging `docs/project/plan`, `docs/project/prioritizer`, `docs/project/documentarian`, and `docs/technology/infrastructure` as `UNLISTED DIR` false positives.

---

#### Proposal 2: Add the "never squash merge" default to `core/git-discipline.md`
**Pattern:** `docs/inbox/never-squash-merge-and-cross-repo-discipline.md` records a developer-stated durable preference (2026-08-05): regular merge (`gh pr merge --merge`) is the paved-road default across all shmorch-touched projects, because AI-authored code needs the commit-by-commit history as an audit trail; rebase merge is acceptable when linear history is wanted; squash is never used except where a project's own decision doc explicitly diverges. The cross-repo-discipline half of this inbox item is already implemented (see "Already addressed" below) but the merge-strategy half was never applied to `core/git-discipline.md` — it's still sitting in the inbox, unaddressed, four days later.
**Frequency:** Single inbox item, but it's an explicit standing developer instruction awaiting a mechanical follow-through — not speculative scope.
**File:** `core/git-discipline.md`, new section alongside "The Two Invariants" (after line ~15, before "## Rules" or as a new subsection under it).
**Change:** Add a section, e.g.:
```
### Merge strategy: never squash

Default merge strategy for all shmorch-touched projects: `gh pr merge --merge` (regular
merge, preserving commit history). Rebase merge is acceptable when a linear history is
wanted. Squash merge is never used by default — squashing destroys the commit-by-commit
story of how AI-authored code evolved, which is the audit trail a human reviewer relies on.

This assumes commit discipline is already being followed (small, single-concern commits,
real messages) — an undisciplined branch's merge-commit noise is a separate problem to fix
at the commit level, not by squashing it away.

A project may override this default with its own reasoning, recorded in that project's
`docs/project/process/index.md` or a decisions doc.
```
Then delete `docs/inbox/never-squash-merge-and-cross-repo-discipline.md` (or trim it to just the resolved cross-repo-discipline note, matching the pattern used elsewhere of removing fully-addressed inbox items).
**Improvement:** Closes a specific developer-stated preference that's been sitting unimplemented since 2026-08-05; removes the inbox item entirely once both halves are done.

---

#### Proposal 3: Finish the build.md Step 3b reframe from `build-md-task-agent-protocol.md`
**Pattern:** The inbox item (2026-04-27, still present) asked for two changes to `workflows/build.md`: (a) richer DoD checklist language in Step 4, and (b) reframing Step 3b's rigid "Call Task (parallel implementers)" decision rule to lean toward Task by default, with an explicit inline-is-the-exception clause. Checking current `build.md`: (a) is done — the DoD gained the "fill in ↑ source and → destination links... if you can't fill in the destination, the track isn't scoped" guard (line 252) and a "Checklist Summary" block (line 266), matching PR #81's scope. But (b) was never applied — `build.md:139` still reads `## Step 3b — Large feature: Call Task (parallel implementers)` and line 141 still says flatly "Identify up to 4 independent modules. For each, call Task." with no exception clause for tightly-entangled modules and no "Default is Task; inline is the exception" framing. This inbox item is half-done and should either be finished or explicitly re-scoped.
**Frequency:** One inbox item, but a clean partial-completion gap (one of its two asks landed via PR #81, the other didn't) — worth closing out now that it's the exact class of leftover work self-improve exists to catch.
**File:** `workflows/build.md`, lines 139–141.
**Change:** Rename the heading and rewrite the opening rule:
```
## Step 3b — Large feature: spawn Task agents

Default is Task agents — one per independent module, up to 4. Call Task for each module
unless the module is so tightly entangled with in-flight context that fully briefing an
agent would cost more than implementing it directly; in that case implement it inline and
note why. Parallel calls are allowed when modules don't share write targets.
```
**Improvement:** Closes the second half of a stale, half-applied inbox item; gives Step 3b the same "default X, inline is the exception" clarity the DoD checklist already has, reducing case-by-case re-litigation of whether to spawn a Task agent.

---

### Already addressed

- **Cross-repo discipline (half of `never-squash-merge-and-cross-repo-discipline.md`):** "No session outside `$SHMORCH_HOME` should branch, commit, or push against the skill repo directly" — implemented in `core/operations.md` § "Cross-repo discipline — actor model, not direct manipulation" (lines 87–99) and referenced in `workflows/self-improve.md` Step 6. Landed via PR #98 (2026-08-05, per session.md catch-up entry). The file itself already notes this as resolved with a partial-marker; only the merge-strategy half (Proposal 2 above) remains open.

- **Self-improve PRs stacking up unmerged (9 open PRs, 2026-07-30):** `workflows/self-improve.md` now lists open PRs by number/title/age before opening a new one — added via PR #89 (2026-08-04), per session.md's 2026-08-01–08-04 catch-up entry, sourced from a DarkBadge self-improve run surfacing the same pattern.

- **VERSION collisions during sequential PR merges:** Recurred across the 2026-07-30/07-31 batch-merge session (11+ PRs, every branch independently bumping VERSION off stale main) and again in the 2026-08-04 semver PRs (#92/#93) and 2026-08-09 (PR #100 rebase, 1.0.4→1.0.6). This is now a documented, expected mechanical step rather than a gap: `core/git-discipline.md`'s conflict-resolution rule (added 2026-07-31, refined in the 2026-08-04 semver migration per session.md) states VERSION conflicts during a merge sequence are expected — take main's value, apply the rebasing branch's own tier. Continued recurrence is the rule working as designed, not unaddressed friction.

- **Stub tracks colliding on shared plan/backlog files (`stub-tracks-shared-file-collision.md`, 2026-07-29):** The inbox item's own suggested "registry-based" mitigation — "a registry with one line/entry per track file... would remove this class of collision entirely" — is exactly what `docs/project/plan/` (directory-of-files registry, replacing the old prose `plan.md`) implements, shipped one day later via PR #78 (2026-07-30, "fixes concurrent stub-track merge collisions" per session.md). Confirmed current `docs/project/plan/index.md` states explicitly: "Backlog items live as individual files in this directory... so concurrent work never collides on a shared edit — add a new file, don't edit this index." Also confirmed no `docs/project/tracks/index.md` registry exists (tracks are directory-scanned), so that half of the original collision risk was never introduced. The inbox file itself was never deleted/updated to reflect this — recommend removing `docs/inbox/stub-tracks-shared-file-collision.md` as a trivial cleanup (not counted as one of the numbered proposals above since it requires no design work, just recognizing it's already resolved).

### No-action observations

- **`backlog-dependency-stack.md` (dependency-stack backlog model):** Single occurrence, explicitly flagged by its own author as "very loose for now — worth revisiting if dependency chains become common." No evidence of a second occurrence since 2026-05-07. Consistent with deferring speculative scope rather than folding into current proposals — leave in inbox for a future run to reassess if dependency chains recur.

- **Sessions ending via stop-hook auto-close, requiring session.md "catch-up" entries:** Seen at least 4 times in timelog (2026-06-17, 2026-07-14, 2026-07-17, 2026-07-21) plus several PR-merge catch-up gaps in session.md (2026-07-17/18, 2026-08-01–04, 2026-08-04–05). `go.md` CW-8 already tracks and surfaces a count-based escalation ("3 or more" auto-closed/auto-wrapped sessions) at orient time, and `orient.md` Step 2's git-log-vs-session.md-date comparison is the designed self-healing mechanism that produces these catch-up entries. Since a detection-and-repair mechanism already exists and is functioning (catch-up entries are getting written, not silently lost), this reads as the system working as designed rather than an unaddressed gap — flagging for visibility only, not proposing a change this run.
