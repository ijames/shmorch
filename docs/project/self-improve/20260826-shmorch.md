### Self-Improve Proposals — 2026-08-26 | Project: shmorch

No open proposals this run — the one inbox candidate evaluated below has already landed on
disk since the task was framed. See "Already addressed."

### Already addressed

**Em-dash rule scope + doctrine-liveness gap (`docs/inbox/em-dash-rule-scope-and-liveness-gap.md`).**
Filed 2026-08-25: `core/engineering-standards.md:14`'s em-dash ban was narrowed by a
project-level override (`shming.com/.shmorch/AGENTS.md` restated it as "this repo," dropping
the global "wherever it lands" scope), and the narrower restatement itself sat unmerged on an
open PR for a day while work continued against `main`. Net effect: em dashes shipped in a real
cover letter and ~70 more instances across a sibling private repo never covered by "this repo"
wording. The inbox item proposed two specific, minimal edits — evaluated as fully supported by
the evidence and recommended for acceptance without modification. Both are now present on
disk:
- `core/engineering-standards.md:14` — the writing-conventions bullet now reads "...commit
  messages meant for human review — including generated content in any sibling or private data
  repo the project writes to, not just the repo this doctrine lives in. A project-level override
  may add detail; it may not narrow this scope." (verified in file, current text).
- `core/operations.md` — a new "**Rule liveness:**" paragraph (lines 85-89) now states: "a
  written rule binds only once merged to the branch the project treats as canonical. A rule
  sitting on an open PR is not in effect... Before treating any `.shmorch/AGENTS.md` or
  `core/*.md` change as active, confirm it's merged, not just committed." (verified in file,
  current text).
No further action — this proposal is implemented, not pending. The inbox item can be closed/
archived.

**PR-cap violation (9 unmerged PRs, 2026-07-30 → 2026-07-31).** Timelog shows
`self-improve: complete — 9 changes applied (PRs #75-#84, unmerged)` sitting open for roughly a
day before a batch merge session the next day. `core/git-discipline.md` § "When Multiple
Branches Open" already caps open branches at 2-3 and says "If you find yourself with 4+ open
PRs, merge or close them before opening new work" — but that clause predates or was
insufficient to prevent this specific incident. The gap has since been closed at the
enforcement point that actually matters: `workflows/self-improve.md` Step 6 now requires
listing every open PR with number/title/age before presenting new proposals, and explicitly
stops the proposal loop "If already at 2-3+ open PRs (the cap in `core/git-discipline.md`)."
Every self-improve run after 2026-07-31 (7/31: 2 PRs, 8/9: 1 PR, 8/17: 2 changes) stays within
the cap, and `docs/project/session.md`'s 2026-08-17 entry shows the gate firing in practice
("3 open PRs awaiting merge — at the self-improve cap, hold further proposals until these
land."). No further action needed.

**Sessions ending via stop-hook auto-close instead of explicit `/shmorch wrap`.** Timelog tail
shows this recurring well past the 2-occurrence threshold (2026-06-17, 07-14, 07-17, 07-21,
08-04, 08-09 ×2, 08-10, 08-19, 08-20). This looks live on the surface, but `workflows/go.md`
already has a full catch-up mechanism for it (Steps CW-1 through CW-8): it reconstructs
session.md from git log, updates plan/, graduates closed tracks, commits state, and — at CW-8
specifically — counts `auto-closed by stop hook` + `auto-wrapped (guard: consecutive
SESSION_START)` occurrences in the timelog and surfaces a named, non-blocking observation once
the count hits 3 or more ("consider whether the wrap trigger itself needs attention"), then
deliberately doesn't repeat the nudge every session. The pattern the task asked to flag is
exactly the condition CW-8 was built to detect and surface. No new proposal warranted; if
CW-8's nudge isn't visibly firing in practice, that's a verification question for a live
session, not a doctrine gap.

### No-action observations

**Scaffold reverse-check "UNLISTED DIR" findings — category mismatch, not drift.** The check
in `workflows/self-improve.md` Step 3 (canonical `EXPECTED_DOCS` vs. actual `docs/` tree) is
designed for calling projects and was run here against `$SHMORCH_HOME` itself — a mismatch the
orchestrator already flagged going in. Verified all three flagged dirs are legitimate,
referenced additions:
- `docs/project/self-improve` — write target named explicitly in `workflows/self-improve.md`
  (Step 4's Task prompt and Step 5's gate check).
- `docs/project/session` — the split-archive dir named in `core/documentation.md`'s
  "session.md growth" rule (added per PR #119).
- `docs/reference/learning` — the project-local learning-log dir named throughout
  `workflows/learn.md`.
No action: this is a false-positive of the check itself when pointed at the skill repo, not a
missing entry in `EXPECTED_DOCS` or `.shmorch/project_docs_log.md` (neither applies to
`$SHMORCH_HOME`'s own tree). Not worth hardening the check to special-case its own home repo
unless it starts producing false positives when run from a real calling project.

**orient/reorient merge-then-unmerge (2026-08-10).** `commands/reorient.md` and
`workflows/reorient.md` were created, then folded back into `orient.md` in the same session
before the originating PR (#102) even merged. This reads as fast, self-corrected iteration
within one session rather than a doctrine gap — no repeat of the same split/merge cycle in any
later session. One occurrence; not a pattern.

**VERSION-tier correction (2026-08-18).** `operations.md: split MAJOR/MINOR criteria... ;
downgrade session.md growth rule to 2.1.0 (MINOR)` shows a bump-tier mistake caught and fixed
same-session. One occurrence in the reviewed window; the VERSION bump rule in
`core/operations.md` already lays out MAJOR/MINOR/PATCH criteria in detail post-fix. No further
action.

**self-improve skipped as "ran-recently-no-new-sessions" (2026-08-17).** Single occurrence;
this is the gate (`tools/check-self-improve-gate.sh`) working as designed, not friction.
