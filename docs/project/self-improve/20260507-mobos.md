### Self-Improve Proposals — 2026-05-07 | Project: MoBoS (mobos)

#### Proposal 1: Verify spec against code before writing validation tests

**Pattern:** Spec had SELLS/BUYC price formulas inverted vs. the actual code. Discovered
mid-session when comparing spec claims to code during test authoring. Required user
clarification before assertions could be written correctly. The 95% confidence interview
checks understanding of *what the user wants built*, but not *what existing code actually does*
when the task is to write tests for existing behavior.

**Frequency:** Once this session; but "write tests to verify X" is a common task type.

**File:** `~/.claude/skills/shmorch/workflows/build.md` — Before Starting section or Step 6 DoD

**Change:** Add to the 95% confidence interview: "If the task is to write tests for *existing*
behavior (not TDD), read the code first and verify any spec/doc claims match the implementation.
Surface discrepancies before writing a single test assertion."

**Improvement:** Catches spec/code divergence before wasted test authoring, not mid-way through.

---

#### Proposal 2: Add PHPUnit vs Behat decision rule to build.md

**Pattern:** User had to ask "should these be Gherkin/Behat or PHPUnit?" The answer was
clear in hindsight (internal calculation logic → PHPUnit; loop lifecycle → Behat) but
no guidance existed. Required a question+answer cycle before work could start.

**Frequency:** Once this session; likely recurring as the test suite grows.

**File:** `~/.claude/skills/shmorch/workflows/build.md` — Step 6 (Tests section of DoD)

**Change:** Add a two-line rule:
- Behat: order loop lifecycle behavior, user-facing outcomes, integration sequences with ScriptedTrader
- PHPUnit: internal calculation logic (price selection, quantity rules), unit-level method behavior, injectable components

**Improvement:** Answers the framework question without asking the user.

---

### No-action observations

- `/clear` mid-session wiped context and caused brief thrash. Already addressed by wrap convention;
  recurring user behavior, not a workflow gap Shmorch can fix.
