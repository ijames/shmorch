### Self-Improve Proposals — 2026-08-09 | Project: appadd

#### Proposal 1: Reverse-scaffold log (`project_docs_log.md`) goes stale silently when a logged directory is renamed
**Pattern:** `.shmorch/project_docs_log.md` still lists `docs/state/documentarian` and
`docs/state/prioritizer` (logged 2026-07-17 per session.md). The `docs/state/` → `docs/project/`
rename landed in PR #123 (2026-07-29, per session.md's 2026-07-28 entry: "the taxonomy backfill
(PR #123, 2026-07-29) renamed `docs/state/` to `docs/project/`"). The log was never updated for
the rename. Running this session's structural reverse-scaffold check against the real
`docs/project/` tree now produces 4 false-positive `UNLISTED DIR` hits — `docs/project/documentarian`,
`docs/project/prioritizer`, plus two more legitimate-but-never-logged dirs created since
(`docs/project/plan_archive`, `docs/project/session_archive`, added per the 2026-07-21 session.md
entry's session/plan file-size discussion) — because the log's path prefix no longer matches
anything on disk.
**Frequency:** 1 confirmed stale-rename occurrence (11+ days undetected: 2026-07-29 rename to
today), plus 2 additional never-logged dirs from the same root cause (nothing re-validates the
log against disk after it's written). Root cause — Step 2.1 in `auto-update.md` only *appends*
to the log; nothing re-checks prior entries for validity — is structural, not incident-specific.
**File:** `$SHMORCH_HOME/workflows/auto-update.md`, Step 2.1 (Reverse scaffold check), around
line 161–176.
**Change:** Before running the reverse-scaffold `find` diff, first validate existing
`project_docs_log.md` entries still exist on disk (`[ -d "$logged_path" ]`); flag any that don't
as "STALE LOG ENTRY — path no longer exists, was it renamed?" rather than silently leaving them
to cause downstream false positives. Also fix this project's log now: replace
`docs/state/documentarian` → `docs/project/documentarian`, `docs/state/prioritizer` →
`docs/project/prioritizer`, and append `docs/project/plan_archive` and
`docs/project/session_archive`.
**Improvement:** A rename or restructure (which this project has done before — `docs/state` →
`docs/project`, and `decisions.md` → `decisions/<topic>.md`) currently corrupts the reverse-scaffold
check invisibly until a human happens to run it and manually notices the mismatch, as happened
here. Self-healing the log removes a recurring source of noisy false positives on every future
auto-update run.

### Already addressed
- **`auto-wrapped on reentry` recurring on almost every session boundary** — timelog shows 22
  occurrences but all dated 2026-06-03 through 2026-07-14; zero occurrences after that. Matches
  the "go.md auto-wrap escalation" fix applied via self-improve 2026-07-18 (skill PR #58, per
  session.md line ~427). Confirmed resolved by the evidence, not just claimed.
- **Self-improve skill-repo PRs not cross-checked against prior unmerged PRs** — proposed in the
  2026-08-04 self-improve run (Proposal 2) and applied: `$SHMORCH_HOME/workflows/self-improve.md`
  line 198 now instructs `gh pr list --repo <skill-repo> --state open` before generating new
  proposals. Confirmed present in the current workflow file.
- **Track-status left `Open` after PR merge, deferred to "next session"** — proposed in the
  2026-08-04 self-improve run (Proposal 1), targeting `wrap.md` Step 6. Not independently
  re-verified against a new occurrence this run (no new instance in the 2026-08-04 session.md
  entry reviewed — that track closed cleanly), so treated as addressed rather than re-proposed.

### Inbox cleanup — 2026-08-09
Removed: 0 files. Kept: 1 file (`feature-flag-dependency-chains.md`, unaddressed — already names its own destination/mechanism, next real graduation candidate).

### No-action observations
- **Tracks scoped in one session but developer explicitly defers starting them** (New Home Page,
  2026-07-14; Amazon affiliate-links + Structurizr diagram, 2026-08-04) — this reads as normal,
  deliberate developer pacing ("wrap and merge this branch first, then explicitly deferred... at
  the end of this session"), not friction. Not proposing a change; flagging only so a future run
  doesn't mistake healthy scope discipline for a stall if it recurs with negative framing.
- **Inbox item `feature-flag-dependency-chains.md`** is a live, substantial doctrine proposal
  (add a "Dependency Chains" section to `core/progressive_delivery.md`) explicitly deferred by
  its own author "for a deliberate skill-file PR rather than doing it inline here." Not a
  self-improve target (it already names its own destination and mechanism) — noting so it isn't
  lost; it's the next real inbox graduation candidate.
</content>
