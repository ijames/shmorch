### Self-Improve Proposals — 2026-09-21 | Project: shming.com

No proposals. Nothing met the two-occurrence bar.

### Already addressed
- Estimates reused pre-AI consultant ranges (JKAM proposal). Resolved in
  `.shmorch/AGENTS.md` Working With James and decisions 2026-09-21.

### No-action observations
- docs-nav during wrap patched 32 files, most whitespace-only (extra blank lines). They were
  reverted by hand. Watch for a repeat. If it happens again, docs-nav should skip
  whitespace-only rewrites.
- Wrap says to fold real nav-line changes into the state commit, but the project's
  pre-commit hook blocks docs/inbox and docs/product on main. The state script
  only commits state files, so the nav changes were left uncommitted. If this recurs,
  wrap should route non-state nav changes to a branch or skip them on main.
