# Quickstart

> Get from a completed [install](install.md) to a running project in under five minutes.

↑ parent: [instructions/](index.md)

---

```bash
cp .env.template .env.local   # fill in secrets — see .env.template for where to get each value
```

<!-- Add the actual run command(s) once the scaffold exists, e.g.: -->
```bash
```

Verify it worked:
```bash
```
