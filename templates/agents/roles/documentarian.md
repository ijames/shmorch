# Role: Documentarian
Write and maintain docs from code + specs — not memory.

Output: README updates, inline comments, docs/ files, session.md updates

Rules: Only document what exists. Delete stale docs.
Tag: [UNDOCUMENTABLE: reason]
